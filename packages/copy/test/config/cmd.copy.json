{
    "options": {
        // "quiet": true,
        "overwrite": false,
        "dest": "./packages/copy/test/tmp2"
        // cwd is project root, since the command is executed from the root.
    },
    "paths": [
        "lerna.json"
    ]
}